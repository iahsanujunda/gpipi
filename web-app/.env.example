# Leave blank in local development so Vite proxies /api to Ktor.
# Set this to the public Ktor origin for a separately hosted production build.
VITE_API_URL=

# Optional explicit Slack deep link used by the app-bar back button.
# When blank, the app first tries browser history and then opens Slack generically.
VITE_SLACK_RETURN_URL=
